{
  "title": "HA GitOps",
  "config": {
    "step": {
      "user": {
        "title": "HA GitOps",
        "description": "Link this Home Assistant configuration directory (/config) to a Git remote using SSH. A deploy key with access to the repository must already exist at the SSH path (or use the default under .ha_gitops/).",
        "data": {
          "repo_url": "Git remote URL (SSH, e.g. git@github.com:owner/repo.git)",
          "branch": "Branch name",
          "git_author_name": "Git commit author name",
          "git_author_email": "Git commit author email",
          "ssh_key_path": "Private SSH key: leave empty for .ha_gitops/id_ed25519 under the config dir, or enter an absolute path, or a path relative to the config directory (e.g. .ha_gitops/id_ed25519)"
        }
      }
    },
    "abort": {
      "already_configured": "HA GitOps is already configured. Only one instance is supported."
    },
    "error": {
      "git_error": "Could not initialize the Git repository. Check the URL, branch, SSH key path, and Home Assistant logs.",
      "unknown": "Unexpected error. Check the logs."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA GitOps",
        "description": "Change the Git remote, branch, commit author, SSH key path, or how often the sync status sensor is polled. Settings are checked with GitManager.initialize() before saving; the integration reloads afterward.",
        "data": {
          "repo_url": "Git remote URL (SSH, e.g. git@github.com:owner/repo.git)",
          "branch": "Branch name",
          "git_author_name": "Git commit author name",
          "git_author_email": "Git commit author email",
          "ssh_key_path": "Private SSH key: leave empty for .ha_gitops/id_ed25519 under the config dir, or enter an absolute path, or a path relative to the config directory (e.g. .ha_gitops/id_ed25519)",
          "scan_interval": "Status sensor poll interval (seconds, 30–86400)"
        }
      }
    },
    "error": {
      "git_error": "Could not initialize the Git repository. Check the URL, branch, SSH key path, and Home Assistant logs.",
      "unknown": "Unexpected error. Check the logs."
    }
  },
  "services": {
    "commit": {
      "name": "Commit",
      "description": "Stage allowed YAML and .gitignore, then create a local commit without pushing.",
      "fields": {
        "message": {
          "name": "Commit message",
          "description": "Optional full git commit message. Leave empty to use the adaptive HA GitOps template from changed files."
        }
      }
    }
  },
  "issues": {
    "pulled_config_reload": {
      "title": "HA GitOps: configuration updated from Git",
      "description": "A pull merged {count} changed file(s) into `/config`. Reload core configuration so YAML changes take effect.\n\nChanged: {files}",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Reload core configuration?",
            "description": "This runs the **homeassistant.reload_core_config** service (same as Developer tools → YAML → Reload configuration). Submit to apply and close this repair."
          }
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "sync_status": {
        "name": "Sync status"
      },
      "local_commit": {
        "name": "Local commit"
      },
      "remote_commit": {
        "name": "Remote commit"
      },
      "changed_files": {
        "name": "Changed YAML files"
      },
      "last_sync": {
        "name": "Last remote sync"
      }
    },
    "button": {
      "pull": {
        "name": "Pull"
      },
      "fetch": {
        "name": "Fetch"
      },
      "push": {
        "name": "Push"
      }
    }
  }
}
